import Placeholder from './Placeholder';

// Settings hub (docs/05): searchable, Global → Character → Chat override scope.
export default function Settings() {
  return (
    <Placeholder title="Settings" note="Appearance · Chat Display · Providers · Presets · Memory · Data — Phase 1+." />
  );
}
